{
  "name": "kernl-order-api-hello-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "npm run build && npx cdk synth",
    "deploy": "npm run build && npx cdk deploy --require-approval never",
    "destroy": "npm run build && npx cdk destroy --force"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "aws-cdk": "2.1114.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.244.0",
    "constructs": "^10.5.0"
  }
}
