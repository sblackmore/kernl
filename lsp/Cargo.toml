[package]
name = "kernl-lsp"
version = "0.1.0"
edition = "2024"
description = "Language server for kernl"
license = "MIT"

[[bin]]
name = "kernl-lsp"
path = "src/main.rs"

[dependencies]
kernlc = { path = "../compiler" }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
